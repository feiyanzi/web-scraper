# 网页抓取助手 启动修复记录

**日期**: 2026-05-25
**问题**: 双击桌面快捷方式 `网页抓取助手.app` 后提示"服务器启动失败"

---

## 问题分析

### 错误日志
```
/opt/homebrew/Caskroom/miniconda/base/bin/python3: can't open file 'server.py': [Errno 1] Operation not permitted
```

### 根因
macOS 对 `.app` 包启动的进程施加安全沙箱限制（`EPERM`），导致：
1. `.app` 内的 bash 脚本直接调用 miniconda python3 时被拒绝文件访问
2. `com.apple.provenance` 扩展属性进一步限制了 `.app` 的执行权限

---

## 修复措施

### 1. 修改启动器（launcher v1.3 → v1.4）
- **改用 `osascript` 调用 Terminal.app** 运行服务器，绕过 `.app` 沙箱限制
- **改用项目 venv 的 python3**（`venv/bin/python3`）替代 miniconda 全局 python3，路径更可靠

### 2. 移除隔离属性
- 执行 `xattr -cr` 清除 `.app` 的 `com.apple.provenance` 扩展属性

### 关键代码变更

**旧方式**（被沙箱阻止）:
```bash
cd "$PROJECT_DIR"
nohup "$PYTHON3" server.py > /tmp/web-scraper.log 2>&1 &
```

**新方式**（通过 Terminal 运行）:
```bash
osascript <<APPLESCRIPT
tell application "Terminal"
    do script "cd '$PROJECT_DIR' && '$PYTHON3' server.py > /tmp/web-scraper.log 2>&1 & sleep 2 && open '$SERVER_URL' && exit"
end tell
APPLESCRIPT
```

---

## 修改文件
- `网页抓取助手.app/Contents/MacOS/launcher`

## 验证结果
- venv python3 可正常读取 `server.py` ✅
- 服务器在 `localhost:5555` 启动成功 ✅
- `/api/config` 接口正常返回 ✅

---

## 后续问答：以后再出现类似情况怎么办

**Q: 如果以后 `.app` 又启动失败了，怎么排查？**

### 第一步：看日志
```bash
cat /tmp/web-scraper.log
```
日志通常会说明具体失败原因。

### 第二步：根据错误类型对症下药

| 错误 | 原因 | 处理 |
|------|------|------|
| `Operation not permitted` | macOS 沙箱限制 | `xattr -cr xxx.app` 清除隔离属性 |
| `No such file or directory` | 路径变了（项目挪了位置） | 改 launcher 里的 `PROJECT_DIR` 路径 |
| `Address already in use` | 端口被占用 | `lsof -i :5555` 找到并 kill 旧进程 |
| `ModuleNotFoundError` | 依赖缺失 | `cd 项目目录 && venv/bin/pip install -r requirements.txt` |

### 核心原则
macOS `.app` 包有安全沙箱，**凡是需要在文件系统上做读写的操作，交给 Terminal 去跑**，不要在 `.app` 内部直接执行。本次修复的核心就是通过 `osascript` 让 Terminal 代为启动服务器。
