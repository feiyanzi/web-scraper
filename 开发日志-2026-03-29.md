# 网页抓取助手 - 开发日志

> 日期: 2026-03-29
> 项目路径: `./web-scraper-project/`

---

## 一、本次更新内容

### 1.1 fxtwitter API 集成

**功能**: 使用 fxtwitter API 获取 Twitter/X 推文完整数据

**API 端点**: `https://api.fxtwitter.com/status/{tweet_id}`

**新增函数**:
```python
def is_twitter_url(url)          # 检测 Twitter/X 链接
def extract_tweet_id(url)        # 提取推文 ID
def fetch_tweet_via_fxtwitter(url)  # 调用 API 获取数据
def twitter_article_to_markdown(tweet_data)  # 解析 Article 内容
```

**支持内容**:
- 普通推文文本
- Twitter Article 长文章
- 推文图片
- 作者信息
- 互动数据（点赞、转发、回复）

### 1.2 Twitter Article 支持

**功能**: 完整解析 Twitter Article 长文章

**解析内容**:
- 文章标题（作为文件名）
- 文章正文（支持 Bold、Italic、链接等样式）
- 封面图片
- 内嵌图片（从 `media_entities` 提取）

**Markdown 格式转换**:
- `blockquote` → `> 引用`
- `header-one` → `# 标题`
- `header-two` → `## 标题`
- `unordered-list-item` → `- 列表`
- `Bold` → `**粗体**`
- `Italic` → `*斜体*`
- 链接 → `[文本](URL)`

### 1.3 图片下载增强

**功能**: 自动下载 Twitter 文章中的所有图片

**图片来源**:
1. 封面图片 (`cover_media`)
2. 文章内嵌图片 (`media_entities`)

**图片质量**: 自动添加 `?name=orig` 获取最高质量

**保存位置**: `{文章标题}/images/`

### 1.4 JSON 数据保存

**功能**: 保存完整推文 JSON 数据

**文件名**: `{标题}_tweet.json`

**包含数据**:
- 推文 ID、URL、创建时间
- 作者信息（ID、名称、头像、简介、粉丝数）
- 互动数据（点赞、转发、回复、收藏、浏览量）
- 文章内容（blocks、entityMap、media_entities）
- 媒体信息（图片 URL、尺寸）

---

## 二、问题修复

### 2.1 服务器状态显示"未连接"

**问题**: 前端调用 `/api/status` 但后端只有 `/api/config`

**解决**: 修改前端代码，调用正确的端点

```javascript
// 修改前
const response = await fetch(`${config.apiServer}/api/status`, ...)

// 修改后
const response = await fetch(`${config.apiServer}/api/config`, ...)
```

### 2.2 重启后服务器启动失败

**问题**: macOS GUI 应用不继承终端环境变量，系统 Python 缺少依赖

**解决**:
1. 创建 Python 虚拟环境 `venv/`
2. 添加 `requirements.txt` 记录依赖
3. 更新启动器脚本，优先使用虚拟环境

**启动器 v2.0 特性**:
- 优先使用 `venv/bin/python3`
- 自动检测并安装缺失依赖
- 启动超时检测（最多等待10秒）
- 详细日志记录到 `/tmp/web-scraper-launcher.log`
- 用户友好的错误对话框

### 2.3 政府网站连接重置

**问题**: 政府网站有反爬机制，连接被重置

**解决**:
1. 添加重试机制（3次，指数退避）
2. 识别政府网站，添加特殊 headers
3. 优先使用 Playwright（如果可用）

### 2.4 Twitter Article 图片位置错误

**问题**: 文件夹重命名后图片在错误位置

**解决**: 调整代码顺序，先重命名文件夹再下载图片

---

## 三、测试案例

### 3.1 Twitter Article 抓取测试

**测试链接**: `https://x.com/PMbackttfuture/status/2038125300085617079`

**抓取结果**:
| 项目 | 数值 |
|------|------|
| 标题 | 我让AI直接操作我的飞书，结果它比我还熟练 |
| 作者 | AI产品黄叔 (@PMbackttfuture) |
| 点赞 | 63 |
| 转发 | 8 |
| 回复 | 19 |
| 图片 | 23 张 |
| JSON | 78KB |

**保存结构**:
```
我让AI直接操作我的飞书，结果它比我还熟练/
├── 我让AI直接操作我的飞书，结果它比我还熟练.md
├── 我让AI直接操作我的飞书，结果它比我还熟练_tweet.json
└── images/
    ├── 01.jpg (封面)
    ├── 02.jpg
    ├── ...
    └── 23.jpg
```

---

## 四、代码变更摘要

### 4.1 server.py 主要变更

**新增函数** (约 100 行):
- `is_twitter_url()`
- `extract_tweet_id()`
- `fetch_tweet_via_fxtwitter()`
- `twitter_article_to_markdown()`
- `is_gov_site()`

**修改函数**:
- `fetch_page_static()` - 添加重试机制
- `fetch_page()` - 政府网站特殊处理
- `scrape()` - Twitter Article 处理逻辑

**新增配置**:
```python
GOV_SITES = ['gov.cn', 'sc.gov.cn', 'people.com.cn', 'xinhuanet.com']

SPECIAL_HEADERS = {
    'gov.cn': { ... }
}
```

### 4.2 新增文件

| 文件 | 说明 |
|------|------|
| `requirements.txt` | Python 依赖列表 |
| `venv/` | Python 虚拟环境 |

### 4.3 修改文件

| 文件 | 变更 |
|------|------|
| `web-scraper-app.html` | 修复 API 端点 |
| `网页抓取助手.app/Contents/MacOS/launcher` | v2.0 重写 |

---

## 五、后续优化建议

### 5.1 高优先级
- [ ] 添加并发图片下载（提升大量图片时的速度）
- [ ] 添加网络请求重试机制到所有请求

### 5.2 中优先级
- [ ] 添加 URL 格式预校验
- [ ] 优化图片过滤逻辑，避免误过滤
- [ ] 添加文件保存的事务性保护

### 5.3 低优先级
- [ ] 添加看门狗自动监控
- [ ] 支持断点续传
- [ ] 将设计模式保存到模式库

---

## 六、API 参考

### fxtwitter API

**端点**: `https://api.fxtwitter.com/status/{tweet_id}`

**响应结构**:
```json
{
  "code": 200,
  "message": "OK",
  "tweet": {
    "url": "https://x.com/...",
    "id": "2038125300085617079",
    "text": "...",
    "author": {
      "name": "AI产品黄叔",
      "screen_name": "PMbackttfuture",
      "avatar_url": "...",
      "followers": 20233
    },
    "likes": 63,
    "retweets": 8,
    "replies": 19,
    "article": {
      "title": "...",
      "content": { "blocks": [...], "entityMap": [...] },
      "cover_media": { ... },
      "media_entities": [ ... ]
    }
  }
}
```

---

## 七、相关链接

- fxtwitter API: https://github.com/FixTweet/FxTwitter
- 飞书 CLI 开源: https://github.com/larksuite/cli
- 项目复盘文档: `项目元反思复盘.md`

---

*本日志由 Claude Code 自动生成*
